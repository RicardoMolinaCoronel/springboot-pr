spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23liRAVt8wfbGpuKDc
            client-secret: 81eb01948c069e1b9adccc7111f80cad093af7db
            scope: read:user
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: GitHub      

        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user

web:
  cors:
    allowed-origins:
    - "http://localhost:5000"
    allowed-methods:
    - GET
    - POST
    - PUT
    - PATCH
    - DELETE
    - OPTIONS
    - HEAD
    max-age: 3600
    allowed-headers:
    - "Requestor-Type"
    exposed-headers:
    - "X-Get-Header"